using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class daochi2 : MonoBehaviour
{
    Rigidbody2D daochi;
    
    // Start is called before the first frame update
    void Start()
    {
        daochi = GetComponent<Rigidbody2D>();
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
